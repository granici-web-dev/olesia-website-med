# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data

# Generated by `prisma generate`, rewritten on every install, and not ours to
# format: reformatting it makes every regeneration a diff (audit A11).
apps/api/src/generated

# The read-only reference design (AGENTS.md R2). Formatting it would be editing
# it, which is the one thing that directory forbids.
apps/frontend/origin

# Written by pnpm, read by nobody.
pnpm-lock.yaml

# Screenshots, and whatever sits beside them.
screenshots
