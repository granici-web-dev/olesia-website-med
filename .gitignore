.DS_Store
node_modules/
.env
.env.local
*.log

# Next.js
.next/
out/

# IDE
.idea/
.vscode/

# Playwright
.playwright-mcp/

# Claude memory (local only)
.claude/
.agents/
skills-lock.json

# npm lockfiles — this repo uses pnpm
package-lock.json
**/package-lock.json


.nx/cache
.nx/workspace-data

# Next.js
.next
out

.nx/polygraph

# Build output & TS incremental info
dist/
*.tsbuildinfo
test-output

vite.config.*.timestamp*
# Uploaded media (storage module — kept on disk / volume, not in git)
apps/api/uploads/
# Same media when the API runs from the workspace root (nx serve cwd)
/uploads/
# Patient document uploads (GDPR — runtime data, never in git)
private-uploads/
.vercel
# Local tooling & scratch artifacts
.codex/
plan-sheet-*.png
# Playwright review captures (local visual QA, not source)
.playwright-mcp/
/*-mobile*.png
/*-desktop*.png
# Extracted Russian text for Textovod proofreading (scratch, regenerated)
/ru-proofread.txt

# raw source video from the client (hero source + TV appearance recordings).
# Never commit these: 30–170 MB each, and GitHub hard-rejects files > 100 MB.
# Compressed/derived copies live in apps/frontend/public/assets; TV appearances
# should be embedded from their original publication URL, not self-hosted.
docs/*.mp4
